
@@ -21,7 +21,7 @@ title: "Hyejin's Lab"
description: "Work hard, be kind, and amazing things will happen."
baseurl: "" # the subpath of your site, e.g. "/blog"
url: "https://yoonhyejin.github.io" # the base hostname & protocol for your site e.g. "https://mmistakes.github.io"
logo: # path of site logo, e.g. "/images/logo.png"
logo: "/images/so-simple-site-logo.png" # path of site logo, e.g. "/images/logo.png"
date_format: "%B %-d, %Y"
read_time: true # reading time estimates, e.g. true
words_per_minute: # 200
